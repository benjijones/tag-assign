###########################
# Example 7: Evolve table #
###########################
# Create table
table evolve -name evolveUsers 
# Add a new field to the existing table
add-field -name comment -type STRING 
# Remove an existing field
remove-field -name phoneNumber
exit
# Add the table plan to the database
plan evolve-table -wait -name evolveUsers

